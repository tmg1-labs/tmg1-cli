[package]
name = "tmg1-cli"
version = "0.2.0"
edition = "2021"
description = "TMG1 video codec CLI — encode/decode/info"

[[bin]]
name = "tmg1"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

[build-dependencies]
cc = "1"
